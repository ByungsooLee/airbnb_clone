<div class="row">
    <% if @photos %>
        <% @photos.each do |photo| %>
            <div class="col-4">
                <div class="card">
                    <%= image_tag photo.image.url, class: "w-100" %>
                </div>
            </div>
        <% end %>
    <% end %>
</div>
